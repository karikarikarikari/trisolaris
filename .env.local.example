# Copy to ".env.local" and fill values locally.
# Do not commit ".env.local" (it is gitignored).

CARTRIDGE_USERNAME=NoamNom
CARTRIDGE_PASSWORD=

# Optional: to let scripts clone private GitHub repos (like welttowelt/yggdrasil-runner)
GITHUB_TOKEN=
